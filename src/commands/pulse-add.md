---
description: v3.0.4 | Add task to Pulse board — creates GitHub issue + board item. Use when user says "add task", "new issue", "pulse add", "track this".
argument-hint: "<title> [--oracle <name>] [--repo <owner/repo>] [--type task|bug|feature] [--priority P0-P3] [--worktree]"
---

# /pulse-add

Execute the `pulse-add` skill with the provided arguments.

## Instructions

**If you have a Skill tool available**: Use it directly with `skill: "pulse-add"` instead of reading the file manually.

**Otherwise**:
1. Read the skill file at this exact path: `~/.claude/skills/pulse-add/SKILL.md`
2. Follow all instructions in the skill file
3. Pass these arguments to the skill: `$ARGUMENTS`

**WARNING**: Do NOT use Glob, find, or search for this skill. The path above is the ONLY correct location. Other files with "pulse-add" in the name are NOT this skill.

---
*🧬 Nat Weerawan × Oracle · Symbiotic Intelligence · v3.0.4*
*Digitized from Nat Weerawan's brain — thousands of hours working alongside AI, captured as code*
